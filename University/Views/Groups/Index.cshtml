@model IEnumerable<Model.Group>

@{
    ViewData["Title"] = "Groups";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-2">
            <h2 class="text-primary">Groups List</h2>
        </div>
        <div class="col-2">
            <a asp-controller="Groups" asp-action="Create" class="btn btn-primary">Add new</a>
        </div>
    </div>

    <hr />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Course Name
                    </th>
                    <th>
                        Group Name
                    </th>
                    <th>
                        Changes
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td width="35%"><a class="nav-link" asp-area="" asp-controller="Groups" asp-action="Index" asp-route-courseId="@item.CourseId">@item.Course.Name</a></td>
                        <td width="35%"><a class="nav-link" asp-area="" asp-controller="Students" asp-action="Index" asp-route-groupId="@item.GroupId">@item.Name</a></td>
                        <td width="30%">
                            <div class="w-100 btn-group" role="group">
                                <a asp-controller="Groups" asp-action="Edit" asp-route-Id="@item.GroupId" class="btn btn-primary mx-1 w-50">Edit</a>
                                <a asp-controller="Groups" asp-action="Delete" asp-route-Id="@item.GroupId" class="btn btn-danger  mx-1 w-50">Delete</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No Groups added yet</p>
    }

</div>

